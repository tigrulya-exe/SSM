name: textRepresentationLike
in: query
required: false
description: >-
  The object's text representation filter. 
  May contain special characters like "/", "'", so should be encoded.
schema:
  type: string
